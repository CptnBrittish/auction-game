#ifndef INVENTORY_H
#define INVENTORY_H

#include <string>

struct item {
    std::string itemName;
    std::string description;
    int quantity;
};

//Add list of item names and description untill we can ship them to a seperate file


#endif
